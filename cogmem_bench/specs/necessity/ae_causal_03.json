{
  "scenario_id": "ae_causal_03",
  "target_type": "action_effect",
  "topic": "keeping a CI pipeline green for a small team",
  "gold_fact": {
    "text": "When the CI pipeline fails on the flaky network integration test, the user re-runs it with the cache disabled, which makes it pass reliably.",
    "fact_type": "action_effect",
    "metadata": {"precondition": "CI fails on the flaky network integration test", "action": "re-run with the cache disabled", "outcome": "the pipeline passes reliably"},
    "fragments": [
      {"session_index": 3, "reveal": "The user describes ONLY the PRECONDITION: their CI intermittently fails on a flaky network integration test. STRICT: do NOT mention re-running, the cache, or the outcome."},
      {"session_index": 6, "reveal": "The user mentions the ACTION and OUTCOME: re-running with the cache disabled makes it pass reliably. Refer to the failure only vaguely ('when it flakes'); do NOT restate which test."}
    ]
  },
  "question": "What does the user do when CI fails on the flaky network test, and what is the result?",
  "gold_answer": "Re-run with the cache disabled; the pipeline then passes reliably.",
  "traps": [
    {"trap_type": "type-confusion", "description": "A one-off session where the user once bumped the test timeout (an experience, not the recurring rule).", "session_index": 1},
    {"trap_type": "volume", "description": "Several unrelated CI tips (parallelism, artifact caching) without causal structure.", "session_index": 5}
  ],
  "session_plan": {"total_sessions": 8, "gold_session_indices": [3, 6]},
  "shared_context": {"trigger": "flaky network integration test fails", "fix": "re-run with cache disabled", "result": "passes reliably"},
  "question_date": "2026-05-24",
  "rationale": "Causal phrasing routes to causal. Answer requires precondition (session 3) + action/outcome (session 6); E10 (no action_effect) must fail if the rule is only action_effect-typed."
}
